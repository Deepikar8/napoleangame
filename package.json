{
  "name": "empire-coalition",
  "version": "1.0.0",
  "description": "Empire & Coalition — a Napoleonic Monopoly-style board game",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
